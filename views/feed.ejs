<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-800 text-white">
    <% include ./partials/navbar.ejs %>
        <h1 class="text-5xl text-center">Feed</h1>

        <div class="container mx-auto mt-8 flex flex-wrap justify-center items-center ">
            <!-- Post Component -->
            <% posts.reverse().map((post, index)=> { %>
                <div class="bg-zinc-700 rounded-lg overflow-hidden shadow-md p-4 w-fit m-2">
                    <!-- Image -->
                    <img src="/images/uploads/<%= post.image %>" alt="Post Image"
                        class="w-64  object-cover mb-4 rounded-md">

                    <!-- Title -->
                    <h2 class="text-lg font-semibold mb-2">
                        <%= post.title %>
                    </h2>

                    <!-- Username -->
                    <p class="opacity-60">@<%= post.user.username %>
                    </p>
                </div>
                <% }); %>
        </div>

</body>

</html>